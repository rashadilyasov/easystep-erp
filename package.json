{
  "name": "easystep-erp",
  "private": true,
  "version": "1.0.0",
  "description": "Easy Step ERP - Frontend in web/, API in api/",
  "scripts": {
    "build": "cd web && npm install && npm run build && cp -r .next ../.next",
    "start": "cd web && npm run start",
    "dev": "cd web && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
